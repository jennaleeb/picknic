<%= form_for(@shop_ingredient) do |f| %>
  <% if @shop_ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop_ingredient.errors.count, "error") %> prohibited this shop_ingredient from being saved:</h2>

      <ul>
      <% @shop_ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shop_id %><br>
    <%= f.text_field :shop_id %>
  </div>
  <div class="field">
    <%= f.label :ingredient_id %><br>
    <%= f.text_field :ingredient_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
